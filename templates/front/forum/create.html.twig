{% extends 'base_front.html.twig' %}

{% block title %}Forum{% endblock %}


{% block body %}
    <div class="forum-container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Soumettre votre question') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}


